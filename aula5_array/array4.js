// Aula 05 -11/06 
// Array - Pop

let cores = ["Azul", "Branco", "Vermelho", "Verde"];
console.log(cores)
console.log(cores.length);

// cores.pop(); // elimina o ultimo elemnteo de um array
// console.log(cores)
// console.log(cores.length);

let ultimoElemento = cores.pop();

console.log(cores);
console.log(cores.length);
console.log(ultimoElemento);